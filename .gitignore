node_modules/
dist/
dist-ssr/
.vercel/
*.local
.DS_Store

# TypeScript incremental build cache. Regenerated by every `tsc -b`, machine
# local, and pure noise in a diff.
*.tsbuildinfo

# Demo screen recordings live on YouTube — never commit the source files again.
*.mp4

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Scratch sandbox for scripts/prep-gallery-images.mjs. Pulls in sharp and
# onnxruntime, which have no business in the site's own dependency tree.
.imgprep/
